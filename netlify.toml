# ============================================================
# Netlify configuration for Pathfinder Music Lessons
# ============================================================

[build]
  functions = "netlify/functions"

# Redirect portal root to login
[[redirects]]
  from = "/portal"
  to   = "/portal/login.html"
  status = 200
